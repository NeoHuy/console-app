version: '2'
services:
    cli:
        build:
            context: ./
            dockerfile: ./cli.docker
        volumes:
            - ./:/code
        links:
            - "cache:cache_server"
    cache:
      image: memcached
      ports:
        - "127.0.0.1:11211:11211"